#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - print the alphabetic
 * Return: Always (Success)
 */

int main(void)
{
	int c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}
