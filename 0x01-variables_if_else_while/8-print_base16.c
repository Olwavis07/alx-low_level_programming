#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main -e entry point
 * Return: Always 0 (Success)
 */

int main(void)

{
	int d;
	char low;

	for (d = '0'; d <= '9'; d++)
	putchar(d);
	putchar('\n');

	return (0);
}
